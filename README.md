# VCP Postgres to AAS

## How to run

Create a `.env` file at the root and fill the necessary
fields.

```dosini
PGDATABASE=
PGHOST=
PGPORT=
PGUSER=
PGPASSWORD=
```

Run docker compose

```sh
$ docker compose up
```
